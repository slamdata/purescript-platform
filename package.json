{
  "name": "purescript-platform",
  "description": "A PureScript interface to platform.js.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/slamdata/purescript-platform.git"
  },
  "author": "Jon Sterling <jon@jonmsterling.com>",
  "scripts": {
    "build": "npm install && gulp bundle-test",
    "example": "npm install && gulp bundle-test && open test/index.html"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-purescript": "^0.5.0",
    "purescript": "^0.7.1",
    "webpack-stream": "^2.1.0"
  },
  "dependencies": {
    "platform": "^1.3.0"
  }
}
